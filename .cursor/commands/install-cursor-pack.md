---
name: install-cursor-pack
description: Install this Cursor pack into the project — interview user and fill Profile during install.
---

# /install-cursor-pack

Alias of the install flow in `CURSOR.md` and `/project-intake`.

**Install = copy + ask + fill in one session**, then use those answers for upcoming work.

Follow `.cursor/rules/install.mdc` and `CURSOR.md` → **Install flow**.
